smartmerge --git --install

* Have some way in Git to indicate not_applicable

Custom merging:
* .po files (implemented); should just use git-po-merge instead?
* NEWS files (implemented)
* GNU ChangeLog (implemented); should just use git-merge-changelog instead
* debian changelog (implemented)
* CSV
 + https://github.com/tabbyrobin/csvdove
 + https://github.com/defcube/csvmerge
* https://github.com/balbuf/composer-git-merge-driver

* python with libCST
* Clang
* https://pypi.org/project/xmldiff/

Preprocessing
* gzip
* Diffoscope

Normalize
* clang-format
* Black
* go-format

Regenerate from source
* configure.ac / configure.in => configure
* Makefile.am => Makefile
* Makefile.in => Makefile
